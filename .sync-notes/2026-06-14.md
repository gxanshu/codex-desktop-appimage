# Weekly upstream sync — 2026-06-14

## Build status

**Validated locally — green where the local toolbox allows; GitHub CI is the
final gate.**

This runner has `node 22`, `python3`, `bash`, `cargo`/`rustc 1.96`, `g++`,
and `7z` available, so most of the suite ran here:

- `cargo check --workspace` — **PASS** (`codex-computer-use-linux`,
  `codex-read-aloud-linux`).
- `cargo check --workspace --locked` — **PASS**, no `Cargo.lock` drift against
  the slim workspace (`members = ["computer-use-linux", "read-aloud-linux"]`,
  no `updater`).
- `node --test scripts/patch-linux-window-ui.test.js` — **254 / 254 pass**.
- `node --test linux-features/*/test.js` — **274 / 274 pass**.
- `bash -n` on `install.sh`, `scripts/build-appimage.sh`,
  `scripts/lib/package-common.sh`, `tests/scripts_smoke.sh`,
  `launcher/start.sh.template` — all clean.
- `tests/scripts_smoke.sh` — **42 / 47 main() tests pass locally.** All 5
  failures **also fail on the pre-merge `origin/main`** — none is a merge
  regression. Every test that exercises a merged file passes.

### CI result on this PR

- **Build App Against Upstream DMG — PASS.** This is the load-bearing job for
  an AppImage fork: it downloads the real upstream DMG and runs
  `make build-app`, so it proves the merged patch set applies and the app
  builds end-to-end.
- **Rust and Smoke Tests — FAIL**, on `test_browser_use_node_repl_fallback_runtime`
  (`Expected file to exist: …/install/resources/node_repl`). This is a
  **pre-existing failure**: `main`'s own HEAD (`6e1a1e4`) fails the same test
  with the same error, and this smoke job has been red on `main` since
  2026-06-01 (syncs #2 and #3 were merged with it red). This PR is at exact
  parity — it introduces no new smoke failure.
  - Root cause (for a future fix, out of scope here): the Browser-plugin
    staging path resolution (`find_browser_plugin_source` /
    `stage_browser_plugin_from_upstream` in `scripts/lib/bundled-plugins.sh`)
    no longer matches the `make_fake_browser_use_upstream_app` fixture, so the
    Browser plugin is skipped before the fallback `node_repl` download runs.
  - The other 4 local failures are masked on CI by this early abort; they fail
    identically on `origin/main` locally and are not merge regressions.

## Upstream commits pulled

85 commits from `upstream/main` (`002aed2..87fd1dd`). Highlights:

- `Add persistent Electron launch flags config` + `CODEX_LINUX_RENDERING_MODE`
  / `CODEX_ELECTRON_DISABLE_GPU_COMPOSITING` launcher work.
- Patch-system refactor: `scripts/patches/main-process.js` split into a
  `main-process/` barrel (`browser.js`, `misc.js`, `quit-lifecycle.js`,
  `tray.js`, `window.js`), strategy telemetry, and build-time critical-patch
  enforcement (`test_patcher_enforce_critical_gate`).
- New core webview patches: `app-server-backfill-wait`, `chat-search-hydration`.
- New opt-in features: `linux-features/frameless-titlebar/`,
  `linux-features/node-repl-reaper/` (both `defaultEnabled: false`).
- Launcher hardening: single-instance lock, IAB sidebar rebound fix,
  cold-start hook runner, resize repaint hook, opaque window surfaces.
- Chrome plugin: active-profile ordering, idle-profile filtering, discarded
  backend close.

## Per-file conflict resolutions

Three content conflicts plus seven modify/delete conflicts.

### Modify/delete — re-deleted (`git rm`)

All belong to subsystems this fork strips; upstream modified them, so they
surfaced as modify/delete and were removed again:

- `flake.nix`, `nix/home-manager-module.nix`, `nix/nixos-module.nix`,
  `docs/nix.md`
- `packaging/linux/control`
- `scripts/ci/container-entrypoint.sh` (containerized `ci-local.sh` runner the
  fork does not ship; only referenced by itself and a smoke assertion that
  lives in deleted test code)
- `updater/src/app.rs`

No *new* deb/rpm/pacman/nix/updater files were introduced by the merge — the
fork's prior deletions held for everything upstream did not touch.

### `README.md` (content)

Took the fork's AppImage-only troubleshooting table. Grafted one genuinely-new
row from upstream — `CODEX_ELECTRON_DISABLE_GPU_COMPOSITING` for resize
ghosting — since it documents a new launcher capability landing in this sync.

### `scripts/lib/package-common.sh` (content)

Took the fork's slim 29-line helper. Upstream added ~850 lines of
deb/rpm/pacman/updater package-builder helpers — none referenced by
`scripts/build-appimage.sh`.

### `tests/scripts_smoke.sh` (content + follow-on fixes)

Nine conflict regions plus two follow-on corrections found by running the
suite:

- **Region 1** (new standalone `test_package_payload_permission_normalization`
  / `test_stage_common_package_files_*`): took **fork**. They drive the
  stripped `package-common.sh` helpers and deleted `packaging/linux/` files.
- **Regions 2–8** (launcher / process-detection / chrome browser-client
  assertions and fixtures): took **upstream**. These exercise fork-kept code
  that the launcher/patcher merge brought up to upstream's current shape.
- **Region 9** (`main()` additions): added `test_process_detection_helper_cmdline_shapes`
  (new, passes, tests `process-detection.sh`). Did **not** add
  `test_launcher_template_sanity` — see below.
- **`test_launcher_template_sanity`**: upstream's version (627 lines) asserts on
  `flake.nix`, `nix/*`, `packaging/linux/*`, `scripts/build-rpm.sh`,
  `scripts/ci/container-entrypoint.sh`, `scripts/install-deps.sh`,
  `updater/*` — all deleted here. The fork keeps a slimmed copy of this test
  but, like `origin/main`, leaves it **dormant** (not called by `main()`).
  Resolving regions 2–6 (all inside this function) as "upstream" produced a
  Frankenstein mixing fork and upstream bodies, and the fork copy also carries
  a stale `seq 1 250` assertion (the launcher uses `seq 1 20`). Fix: restored
  the function to `origin/main`'s exact bytes and kept it out of `main()`.
  Re-syncing this slimmed test against the current launcher is a follow-up.
- **`assert_mode()` helper restored**: upstream added `assert_mode` usage to
  the fork-kept `test_chrome_plugin_staging` (checks staged Chrome scripts are
  `0755`), but the fork had dropped the helper definition as "unused" when it
  stripped the package-permission tests. Re-added the upstream `assert_mode`
  helper so the kept test resolves it.

## AppImage-only invariants (unchanged by this sync)

- `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/release-appimage.yml`
  — byte-identical to `origin/main`. No deb/rpm/pacman/nix targets or jobs.
- `.github/workflows/upstream-build-app.yml` — **reverted to `origin/main`'s
  version.** The merge picked up upstream's portable `awk` header matching
  (mawk-safe on Ubuntu runners) and `if: always()` / `if-no-files-found: ignore`
  on the patch-report upload, but the sync token lacks the GitHub `workflow`
  scope and the push is rejected for any workflow edit (same constraint as the
  2026-06-07 sync). The file is byte-identical to `origin/main`; it still only
  runs `make build-app` and ships no packages. The dropped awk change matters
  only for the CI drift-detection workflow's header parsing — the daily release
  path (`release-appimage.yml`) is unaffected. See follow-up 4.
- `Cargo.toml` — slim workspace preserved.

## Follow-ups

1. Re-sync the fork's slimmed `test_launcher_template_sanity` against the
   current launcher (drop the stale `seq 1 250`, drop the deleted-subsystem
   assertions) and re-enable it in `main()` so launcher drift is caught.
2. The 5 environment-only smoke failures should be confirmed green on CI.
3. `docs/` files inherited from earlier syncs still reference
   deb/rpm/pacman/updater paths — candidate for a slimming pass.
4. Re-apply upstream's `upstream-build-app.yml` awk/header-parsing fix
   (`tolower($0) ~ /.../` instead of `IGNORECASE`, plus `if: always()` on the
   patch-report upload) once a token with the `workflow` scope is available, or
   apply it directly via the GitHub web UI. Without it, mawk on Ubuntu runners
   may mis-parse `Last-Modified` / `ETag` / `Content-Length` headers in the
   drift-detection job.
